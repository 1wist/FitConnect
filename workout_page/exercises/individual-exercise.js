// const exerciseInfo = {
//     name: 'Push-ups', // Retrieve exercise name
//     duration: '30 seconds', // Retrieve exercise duration
//     image: 'pushups.jpg', // Retrieve exercise image
//     pageURL: 'exercises/push-ups.html', // URL of the current exercise page
//   };
  
//   // Load bookmarked exercises from local storage
//   const savedBookmarkedExercises = JSON.parse(localStorage.getItem('bookmarkedExercises')) || [];
  
//   // Check if the current exercise is bookmarked
//   const isBookmarked = savedBookmarkedExercises.some((exercise) => exercise.pageURL === exerciseInfo.pageURL);
  
//   // Use `isBookmarked` to display or manage bookmarks as needed on the page
    
  